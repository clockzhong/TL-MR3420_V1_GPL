#!bin/sh
ln -sf		libc_pic.a       			libc.a
ln -sf		libcrypt_pic.a   			libcrypt.a
ln -sf		libcrypt.so      			libcrypt.so.0
ln -sf		libdl_pic.a      			libdl.a
ln -sf		libdl.so         			libdl.so.0
ln -sf		libm_pic.a       			libm.a
ln -sf		libm.so          			libm.so.0
ln -sf		libnsl_pic.a     			libnsl.a
ln -sf		libnsl.so        			libnsl.so.0
ln -sf		libpthread_pic.a 			libpthread.a
ln -sf		libpthread.so    			libpthread.so.0
ln -sf		libresolv_pic.a  			libresolv.a
ln -sf		libresolv.so     			libresolv.so.0
ln -sf		librt_pic.a      			librt.a
ln -sf		librt.so         			librt.so.0
ln -sf		libutil_pic.a    			libutil.a
ln -sf		libutil.so       			libutil.so.0